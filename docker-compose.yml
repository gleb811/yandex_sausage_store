version: "3.8"

services:
  backend:
    image: ${CI_REGISTRY_IMAGE}/sausage-backend:latest
    container_name: sausage-store-backend
    environment:
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL}
      SPRING_DATA_MONGODB_URI: mongodb://std-024-07:Testusr1234@rc1d-fmpc8y27lrcif391.mdb.yandexcloud.net:27018/std-024-07?ssl=true
    networks:
      - sausage-network

  frontend:
    image: ${CI_REGISTRY_IMAGE}/sausage-frontend:latest
    volumes:
      - /tmp/${CI_PROJECT_DIR}/frontend/default.conf:/etc/nginx/conf.d/default.conf
    container_name: sausage-store-frontend
    restart: always
    ports:
      - "8080:80"
    networks:
      - sausage-network

networks:
  sausage-network: